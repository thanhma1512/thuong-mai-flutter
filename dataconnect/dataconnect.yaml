specVersion: "v1beta"
serviceId: "commerce"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "commerce-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
